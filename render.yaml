services:
  - type: cron
    name: fiyattakip
    runtime: python
    schedule: "0 */6 * * *"
    buildCommand: pip install -r requirements.txt && playwright install chromium
    startCommand: python takip.py